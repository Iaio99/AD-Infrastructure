config:
  core.https_address: {{ cluster_address }}:8443
networks:
- name: incusbr0
  type: bridge
storage_pools:
- name: local
driver: dir
profiles:
- name: default
  devices:
    root:
      path: /
      pool: local
      type: disk
cluster:
  server_name: {{ server_name }}
  enabled: true